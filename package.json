{
  "name": "trainerlogbook",
  "version": "1.0.0",
  "description": "健身教练客户管理系统",
  "scripts": {
    "build": "flutter build web --release",
    "vercel-build": "flutter build web --release"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
